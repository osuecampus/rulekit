/**
 * Generate a Cursor command file from prompt content
 * Cursor commands are plain Markdown files stored in .cursor/commands/
 */
export const generateCursorCommand = (content: string): string => {
  // Cursor commands use plain markdown, no transformation needed
  return content;
};

